CREATE COLUMN TABLE COST_CENTER_EMPLOYEE_RESPONSIBLE(
	"COST_CENTER_EMPLOYEE_RESPONSIBLE_ID" BIGINT CS_FIXED GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	"COST_CENTER_ID" BIGINT NOT NULL,
	"EMPLOYEE_RESPONSIBLE_ID" BIGINT NOT NULL,
	"CREATED_DATE_TZ" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	"MODIFIED_DATE_TZ" TIMESTAMP,
	"CREATED_USER_ID" BIGINT NOT NULL,
	"MODIFIED_USER_ID" BIGINT,
	"ENABLED" TINYINT DEFAULT 1,
	"DELETED" TINYINT DEFAULT 0,
    FOREIGN KEY ("CREATED_USER_ID") REFERENCES "PLANNING_TOOL"."USER" ("USER_ID"),
	FOREIGN KEY ("COST_CENTER_ID") REFERENCES "PLANNING_TOOL"."COST_CENTER" ("COST_CENTER_ID"),
	FOREIGN KEY ("EMPLOYEE_RESPONSIBLE_ID") REFERENCES "PLANNING_TOOL"."EMPLOYEE_RESPONSIBLE" ("EMPLOYEE_RESPONSIBLE_ID")
);

ALTER TABLE COST_CENTER DROP ("EMPLOYEE_RESPONSIBLE_ID");

-- *************************************************************************************
-- Update schema version
INSERT INTO SCHEMA_VERSION(VERSION, DESCRIPTION, SCRIPT)
VALUES('V5.0.0-13', 'Improve settings cost center management', 'V201701201109__Improvement_Settings_Cost_Center_management.sql');

COMMIT;