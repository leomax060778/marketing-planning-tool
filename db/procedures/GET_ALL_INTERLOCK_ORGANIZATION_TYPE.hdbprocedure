PROCEDURE "PLANNING_TOOL"."xsplanningtool.db.procedures::GET_ALL_INTERLOCK_ORGANIZATION_TYPE" ( 
		OUT out_interlock_organization_type TABLE (ORGANIZATION_TYPE_ID BIGINT, NAME NVARCHAR(100))
	) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER 
	DEFAULT SCHEMA "PLANNING_TOOL" AS
BEGIN
	out_interlock_organization_type = SELECT ORGANIZATION_TYPE_ID, name FROM ORGANIZATION_TYPE
							WHERE enabled = 1 and deleted = 0;
END;
