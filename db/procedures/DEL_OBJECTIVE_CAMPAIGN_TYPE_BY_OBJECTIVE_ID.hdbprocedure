PROCEDURE "PLANNING_TOOL"."xsplanningtool.db.procedures::DEL_OBJECTIVE_CAMPAIGN_TYPE_BY_OBJECTIVE_ID" (
	IN in_objective_id bigint,
	OUT out_result integer
)
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	DEFAULT SCHEMA "PLANNING_TOOL" AS
BEGIN
 	DELETE FROM "PLANNING_TOOL"."OBJECTIVE_CAMPAIGN_TYPE_SUBTYPE"
 	WHERE objective_id = in_objective_id;

	SELECT ::ROWCOUNT INTO out_result FROM DUMMY;

END;
