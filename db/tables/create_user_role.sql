CREATE COLUMN TABLE "PLANNING_TOOL"."USER_ROLE"
(
	USER_ROLE_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	USER_ID BIGINT NOT NULL,
	ROLE_ID BIGINT NOT NULL,
	
	CREATED_DATE_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	MODIFIED_DATE_TZ TIMESTAMP,
	CREATED_USER_ID BIGINT NOT NULL,
	MODIFIED_USER_ID BIGINT,
	ENABLED TINYINT DEFAULT 1, 
	DELETED TINYINT DEFAULT 0
);

ALTER TABLE "PLANNING_TOOL"."USER_ROLE" ADD FOREIGN KEY ( "USER_ID" ) REFERENCES "PLANNING_TOOL"."USER" ("USER_ID") ON UPDATE RESTRICT ON DELETE RESTRICT
;

ALTER TABLE "PLANNING_TOOL"."USER_ROLE" ADD FOREIGN KEY ( "ROLE_ID" ) REFERENCES "PLANNING_TOOL"."ROLE" ("ROLE_ID") ON UPDATE RESTRICT ON DELETE RESTRICT
;
