CREATE COLUMN TABLE "PLANNING_TOOL"."APPLICATION_VERSION"
(
	APPLICATION_VERSION_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	VERSION NVARCHAR(100),
	RELEASE_DATE TIMESTAMP DEFAULT CURRENT_TIMESTAMP, 
	RELEASE_NOTES NVARCHAR(512),
	APPLICATION_ID BIGINT NOT NULL,
	CREATED_DATE_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	MODIFIED_DATE_TZ TIMESTAMP,
	CREATED_USER_ID BIGINT NOT NULL,
	MODIFIED_USER_ID BIGINT,
	ENABLED TINYINT DEFAULT 1, 
	DELETED TINYINT DEFAULT 0
);

ALTER TABLE "PLANNING_TOOL"."APPLICATION_VERSION" ADD FOREIGN KEY ( "APPLICATION_ID" ) REFERENCES "PLANNING_TOOL"."APPLICATION" ("APPLICATION_ID") ON UPDATE RESTRICT ON DELETE RESTRICT
;
